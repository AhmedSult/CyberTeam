# تبعيات Streamlit Cloud — نقطة الدخول: streamlit_app.py (جذر المستودع)
# المشروع الكامل (FastAPI + React) يُنشر منفصلاً — انظر README.md
streamlit>=1.40.0
pandas>=2.2.0
openpyxl>=3.1.0
httpx>=0.27.0
